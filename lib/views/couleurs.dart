import 'package:flutter/material.dart';

/// Palette reprise directement des maquettes Figma du projet AppNotes.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFF3AE47); // en-tête, boutons, FAB
  static const Color background = Color(0xFFFDF7CE); // fond crème
  static const Color card = Color(0xFFFFFEFD); // fond des cartes de note
  static const Color danger = Color(0xFFF66F72); // suppression
  static const Color textDark = Color(0xFF2E210D); // titres et texte fort
  static const Color textMuted = Color(0xFF8A7C63); // dates, placeholders
}
